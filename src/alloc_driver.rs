use crate::buffer::BufferState;
use crate::chunk::Chunk;
use crate::config::Config;

pub fn allocate(chunks: Vec<Chunk>, conf: &Config) -> Vec<BufferState> {
    let mut states: Vec<BufferState> = Vec::new();
    states
}
